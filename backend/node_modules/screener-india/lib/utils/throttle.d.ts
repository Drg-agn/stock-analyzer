export declare class Throttle {
    private readonly defaultIntervalMs;
    private lastCallAt;
    constructor(defaultIntervalMs: number);
    wait(scope?: string, intervalMs?: number): Promise<void>;
}
//# sourceMappingURL=throttle.d.ts.map