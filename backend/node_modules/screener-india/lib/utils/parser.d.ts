import { HTMLElement } from "node-html-parser";
import type { TopRatio, FinancialTable, TableRow, AnalysisData, DocumentLink, PeerRow } from "../types.js";
export declare function parseHtml(html: string): HTMLElement;
export declare function parseTitle(root: HTMLElement): string;
export declare function parseTopRatios(root: HTMLElement): TopRatio[];
export declare function parseTable(section: HTMLElement | null): FinancialTable | undefined;
export declare function parseAnalysis(root: HTMLElement): AnalysisData | undefined;
export declare function parseDocuments(root: HTMLElement): DocumentLink[];
export declare function parsePeers(root: HTMLElement): PeerRow[];
export declare function parseSectorLinks(root: HTMLElement): Array<{
    name: string;
    slug: string;
    url: string;
}>;
export declare function parseDataTable(root: HTMLElement): {
    columns: string[];
    rows: TableRow[];
};
export declare function parsePagination(root: HTMLElement): {
    currentPage: number;
    totalPages: number;
};
export declare function parseScreenItems(root: HTMLElement): Array<{
    id: number;
    slug: string;
    title: string;
    description: string;
    url: string;
}>;
export declare function parseSectionIds(root: HTMLElement): string[];
//# sourceMappingURL=parser.d.ts.map